# Copy to .env (gitignored) and fill in. Nothing here is read by the game at
# runtime; these are keys the tools/ scripts use to fetch or generate assets.

# Tripo3D, for tools/fetch_tripo3d.py. Get one at https://platform.tripo3d.ai/api-keys
TRIPO_API_KEY=
