# dependencies
node_modules/
.pnp.*

# build output
dist/
build/
*.tsbuildinfo

# env / local
.env
.env.*
!.env.example

# logs
*.log
npm-debug.log*

# editor / OS
.DS_Store
Thumbs.db
.idea/
.vscode/*
!.vscode/extensions.json

# test / coverage
coverage/
.nyc_output/

# local reference (copyrighted game screenshots, not distributed)
reference-screenshots/

# local agent / session state
.claude/
.agents/
.superpowers/
docs/todo/

# Screenshots staged for the user to review; only approved ones move to devlog/
review/

# Derived spinner frames; the sheet in public/ is what ships
assets/loading/build/

# Downscaled hides and their derived normals; monsters.glb embeds them
assets/monsters/build/

# Bytecode from the tools/ scripts
tools/__pycache__/

# The house rules for the agent working in here. Local only: the repo does not
# hand a contributor a list of instructions written for someone else.
CLAUDE.md
AGENTS.md

# Sounds
audio-libs/
skills-lock.json
.ignore

# Local MCP server config for the agent working here
.mcp.json

# Intermediate fire-sprite frames; the baked sheet in public/ is what ships
.tmp-fire-sprite/

# Teaser build (npm run build:soon -w apps/web); deployed to BM-EXC, never committed
apps/web/dist-soon/
